project xmlns=httpmaven.apache.orgPOM4.0.0 xmlnsxsi=httpwww.w3.org2001XMLSchema-instance
  xsischemaLocation=httpmaven.apache.orgPOM4.0.0 httpmaven.apache.orgmaven-v4_0_0.xsd
  modelVersion4.0.0modelVersion
  groupIdcom.examplegroupId
  artifactIdJtProjectartifactId
  packagingwarpackaging
  version1.0-SNAPSHOTversion
  nameJtProject Maven Webappname
  urlhttpmaven.apache.orgurl

  dependencies
    !-- Servlet API for the web application --
    dependency
      groupIdjavax.servletgroupId
      artifactIdjavax.servlet-apiartifactId
      version3.1.0version
      scopeprovidedscope
    dependency

    !-- JUnit for testing --
    dependency
      groupIdjunitgroupId
      artifactIdjunitartifactId
      version4.13.2version
      scopetestscope
    dependency
  dependencies

  build
    finalNameJtProjectfinalName
    plugins
      !-- Compiler plugin to specify Java version --
      plugin
        groupIdorg.apache.maven.pluginsgroupId
        artifactIdmaven-compiler-pluginartifactId
        version3.8.1version
        configuration
          source1.8source
          target1.8target
        configuration
      plugin

      !-- WAR plugin to package the application as a .war file --
      plugin
        groupIdorg.apache.maven.pluginsgroupId
        artifactIdmaven-war-pluginartifactId
        version3.3.1version
      plugin

      !-- Surefire plugin for running tests --
      plugin
        groupIdorg.apache.maven.pluginsgroupId
        artifactIdmaven-surefire-pluginartifactId
        version2.22.2version
      plugin
    plugins
  build
project

